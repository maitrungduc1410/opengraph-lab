{
  "name": "opengraph-lab",
  "private": true,
  "type": "module",
  "scripts": {
    "gen-images": "node scripts/gen-images.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail og-lab --format json",
    "login": "wrangler login"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
